# fee-schedule.yaml — fee rates loaded by the payments-core service.
#
# Owned by: payments-core engineering. Rate values are set by the Pricing Committee;
# changes to them are expected to reference an approved pricing record.

wire_percent: 0.0025          # WIRE — 0.25%
ach_flat_usd: 0.25            # ACH — flat $0.25
swift_percent: 0.005          # SWIFT — 0.5%
swift_flat_usd: 15.00         # SWIFT — plus $15 flat
rtp_percent: 0.0035           # RTP — 0.35%
rtp_minimum_usd: 2.00         # RTP — USD 2.00 minimum
